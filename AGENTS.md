# AGENTS.md

本仓库的完整 Agent 约束见 [`docs/agent.md`](docs/agent.md)，开始工作前必须阅读并遵守。

项目仍处于开发阶段，不得仅因改动范围大而回避高价值架构修复。根因涉及层次职责、
核心数据模型或跨 pass 所有权时，应先明确边界、不变量、迁移范围和验证方案，再直接
替换旧结构；不要为已确认错误的架构保留兼容层、双轨事实或后层 fallback。只有收益
不足、边界无法证明或验证条件不成立时才缩小方案，并明确说明取舍。
