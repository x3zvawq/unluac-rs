[package]
name = "unluac-cli"
version = "1.0.0"
edition = "2024"
publish = false

[lints]
workspace = true

[dependencies]
clap = { version = "4.6.0", features = ["derive"] }
unluac = { path = "../.." }
