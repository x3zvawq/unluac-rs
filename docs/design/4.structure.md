# StructureFacts

## 目标

StructureFacts 只产出结构候选和结构约束，不直接产出最终源码结构结论。

这层的职责是把：

- low-IR 指令形状
- CFG 上的图事实
- dominator / loop / frontier 结果
- dataflow 的 def-use / phi / effect 信息

转成更贴近恢复工作的候选集合，交给 HIR 去做最终判定。

## 当前规则

- `StructureFacts` 默认基于 reachable 子图分析
- `StructureFacts` 消费 `LoweredProto.instrs`、`Cfg`、`GraphFacts` 和 `DataflowFacts`
- `StructureFacts` 负责识别可结构化候选与必须保留跳转的区域
- `StructureFacts` 产的是 hint、candidate、requirement，不是最终语法

## 数据结构

```text
StructureFacts
  branch_candidates
  loop_candidates
  short_circuit_candidates
  goto_requirements
  region_facts
  scope_candidates

BranchCandidate
  header: BlockRef
  then_entry: BlockRef
  else_entry: Option<BlockRef>
  merge: Option<BlockRef>
  kind: BranchKind
  invert_hint: bool

BranchKind
  IfThen
  IfElse
  Guard

LoopCandidate
  header: BlockRef
  blocks
  backedges
  exits
  continue_target: Option<BlockRef>
  kind_hint: LoopKindHint
  reducible: bool

LoopKindHint
  WhileLike
  RepeatLike
  NumericForLike
  GenericForLike
  Unknown

ShortCircuitCandidate
  header: BlockRef
  blocks
  merge: BlockRef
  result_reg: Option<Reg>
  kind_hint: ShortCircuitKindHint
  reducible: bool

ShortCircuitKindHint
  AndLike
  OrLike
  Unknown

GotoRequirement
  from: BlockRef
  to: BlockRef
  reason: GotoReason

GotoReason
  IrreducibleFlow
  CrossStructureJump
  MultiEntryRegion
  UnstructuredBreakLike
  UnstructuredContinueLike

RegionFact
  blocks
  entry: BlockRef
  exits
  kind: RegionKind
  reducible: bool
  structureable: bool

RegionKind
  Linear
  BranchRegion
  LoopRegion
  Irreducible

ScopeCandidate
  entry: BlockRef
  exit: Option<BlockRef>
  close_points
  kind: ScopeKind

ScopeKind
  BlockScope
  LoopScope
  BranchScope
```

## 边界

- `LoopCandidate.kind_hint` 只是提示，不是最终循环语法结论
- `ShortCircuitCandidate` 只是表达式形状控制流候选，不等于最终 `and/or`
- `RegionFact` 只描述区域事实，不直接决定 AST 结构
- `GotoRequirement` 明确标出无法被当前候选吸收的跳转
- `StructureFacts` 可以直接读取 low-IR 指令形状，但不负责重算 CFG / GraphFacts / Dataflow

## 各类候选的用途

### BranchCandidate

回答：

- 某个 branch 更像 `if-then`、`if-else` 还是 guard
- merge 点在哪里
- 为了得到更自然结构，条件是否值得反转

### LoopCandidate

回答：

- 某个自然循环候选更像前测循环、后测循环还是 `for` 形态
- continue/exit 候选在哪里
- 这是不是一个可规约循环

### ShortCircuitCandidate

回答：

- 某片分支控制流是否更像“值产生型控制流”
- 是否值得优先尝试恢复成短路表达式

这层只给出 `AndLike / OrLike / Unknown` 提示。是否最终恢复成 `a and b` 或 `a or b`，由 HIR 决定。

### GotoRequirement

回答：

- 哪些跳转无法被当前结构候选吸收
- 哪些 block 需要 label
- 哪些边必须保留成显式 goto

这块很关键，因为 HIR/AST 后续不应再重新猜“哪些地方必须保留跳转”。

### RegionFact

回答：

- 某片 block 集合是否是单入口单出口区域
- 是否可规约
- 是否值得继续尝试结构化

### ScopeCandidate

回答：

- 哪些区域可能对应局部作用域边界
- 哪些 loop/branch/close 点会强制形成 scope 切分

## Consumes / Provides

- Consumes: `LoweredProto.instrs`, `Cfg`, `GraphFacts`, `DataflowFacts`
- Provides: `branch_candidates`, `loop_candidates`, `short_circuit_candidates`, `goto_requirements`, `region_facts`, `scope_candidates`

## 后续依赖

HIR 会直接依赖这些事实：

- 先尝试使用 `short_circuit_candidates` 恢复表达式形状控制流
- 再使用 `branch_candidates` 和 `loop_candidates` 做语句级结构恢复
- 使用 `goto_requirements` 决定哪些边不能再继续结构化
- 使用 `scope_candidates` 和 `Close` 相关点建立作用域边界

这样 HIR 的工作重点就变成：

- 在候选之间做最终决策
- 恢复表达式和值流
- 选择最自然的结构形式

而不是回过头再重算底层图事实。
